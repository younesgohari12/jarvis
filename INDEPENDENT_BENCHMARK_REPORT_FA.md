# INDEPENDENT_BENCHMARK_REPORT_FA — JARVIS v22.4.2

> طبقه‌بندی شواهد (§42): هر عدد در این سند دقیقاً با یکی از سه برچسب زیر ارائه می‌شود.
> قانون §38: ادعای «هوش افزایش یافت» صرفاً به‌دلیل افزودن تست/گسترش رجکس/پچ شکست‌های شناخته‌شده **نمی‌شود**؛ نتیجه‌ی تشخیصی مستقل این سند جداگانه گزارش شده و هرگز با بنچمارک فریز میانگین نمی‌شود.

## 1. بنچمارک فریز منتشرشده — [ادعای تاریخی ریلیز]

- `V22_4_FROZEN_EVALUATION` (2075 مورد، SHA-256: `063aef8f…7be1`): **1891/2075 = 91.13%**، answerable-only **1568/1749 = 89.65%**.
- **در v22.4.2 اجرا نشد** (فایل فریز بایت‌به‌بایت دست‌نخورده؛ SHA در `RELEASE_V22_4_2.json` ثبت و مقایسه شد). بنابراین هیچ عدد جدیدی از این دیتاست ادعا نمی‌شود و نتیجه‌ی 91.13% فقط ادعای تاریخی ریلیزهای قبلی است.

## 2. مجموعه تشخیصی نو v22.4.2 — [اندازه‌گیری شده اکنون]

ساخت و فریز (§26): 107 جمله‌ی **تازه‌نویس‌شده** در 10 دامنه (بدون کپی از هر بنچمارک قبلی)، expectedها در زمان نگارش محاسبه و فریز شد، SHA-256 **قبل از هر اجرایی** ثبت شد:
`benchmarks/v22_4_2_diagnostic_set.jsonl` — SHA-256 `8e4aac45df363facdb4cb64f65fc9515111665949941b3e170375f13599adaa2`

روش اجرا: همان روش مرجع (TemporaryRuntime، ابزارها غیرفعال، پاسخ عامل، نمره‌دهی عددی/abstain). دو اجرا انجام شد: run1 پس از W1a، run2 پس از W1b/c/d. هر دو گزارش می‌شوند (بدون دستکاری ردیف‌ها).

| دامنه | run1 | run2 |
|---|---|---|
| work_rate (20) | 6 | 7 |
| ownership (12) | 3 | 3 |
| rates (12) | 5 | 5 |
| finance (12) | 5 | 5 |
| age (10) | 3 | 3 |
| units (10) | 0 | 0 |
| hard_negatives (10, باید abstain شود) | **10** | **10** |
| persian_math (8) | 0 | 0 |
| english_math (8) | 0 | 0 |
| mixed_language (5) | 2 | 2 |
| **score_numeric (کل)** | **31.78%** | **32.71%** |
| **answerable-only** | **24.74%** | **25.77%** |

### تفسیر صادقانه (مهم)

1. **این عدد با 91.13% قابل مقایسه نیست**: بنچمارک فریز روی توزیعی که موتور برایش طراحی/تمرین شده بود بسته شد (65 literal + 570 authored-template + 1440 synthetic — [تاریخی])؛ مجموعه‌ی v22.4.2 عمداً paraphrase-محور و خارج از قالب است تا **پوشش واقعی** را بسنجد. نتیجه: پوشش تعمیم پارچه‌ای، حدود 25–33%.
2. **رفتار abstention سالم است**: hard_negatives 10/10 — موتور جواب غلط نمی‌سازد؛ «نمی‌دانم» می‌گوید (fail-closed در هوش). این مهم‌ترین سیگنال سلامت در این اجراست.
3. **WEAK-W1 (رفع واژگانی)**: اثر واقعی و محدود — ردیف machines (+1) و خارج از مجموعه، خانواده‌های robots/frames و widgets با تأیید کامل اسلات‌ها verified شدند (تست‌های رگرسیون). هیچ ادعای فراتری نمی‌شود.
4. **شکست‌های ساختاری باز** (ریشه‌یابی شده برای V23): نرخ ترکیبی («Ali in 6h، Sara in 3h → together»)، سؤال معکوس ساعت، نرخ هر-کارگر («builds 4 chairs an hour»)، مانده‌ی کار، تبدیل واحد مستقل («How many grams are 2.5 kilograms?» — کلاس سؤال conversion)، حساب تناسبی/باقیمانده، برخی فرم‌های سن. جزئیات ردیف‌به‌ردیف در `reports/v22_4_2/diagnostic.json`.
5. **یافته‌ی محیطی (harness)**: 4 ردیف به‌دلیل تلاش عامل برای فراخوانی ابزار (در بلایند ممنوع) fail شدند — هم‌روش با runner مرجع. این خروجی‌ها «خالی» ثبت شدند نه جواب غلط.
6. یک خطای نویسندگی در فریز (expected منفیِ «father/daughter») شناسایی و **بدون اصلاح فایل فریز** در addendum مستند شد؛ آن ردیف در هر دو تفسیر fail بود و اثر نمره ندارد (`reports/v22_4_2/diagnostic_addendum.json`).

## 3. عملکرد — [اندازه‌گیری شده اکنون]

محیط: Linux x86_64، Python 3.12.14، CPU/رم مجازی‌سازی‌شده — **با اعداد منتشرشده‌ی ویندوزی قابل مقایسه نیست** (§27: مقایسه‌ی بین‌سخت‌افزاری فقط با ذکر محیط).

گزارش کامل: `reports/v22_4_2/performance_warm.json` (cold + warm mean/p50/p95/p99 per family + پیوست GC). نمونه warm:
- simple_math: p50 ≈ 2.72ms، p95 ≈ 2.83ms، p99 ≈ 2.92ms
- ownership: بدون outlier معنادار در steady-state (`runtime_change_required=false`)
- رگرسیون عملکردی ناشی از فیکس‌های امنیتی مشاهده نشد (پارسر route و اسکنر خارج از مسیر داغ پاسخ‌دهی هستند؛ اسکن فقط ابزار ریلیز است).

## 4. رگرسیون و جهش — [اندازه‌گیری شده اکنون]

- رگرسیون کامل نهایی: **1292 passed + 585 subtests، 0 failures، 1 skip محیطی** (تست ZIP بدون ZIP در درخت). خط مبنای 1215 (v22.4) و 1228 (v22.4.1) حفظ شد.
- جهش: 4 جهش جدید M8–M11 همگی KILLED_BY_ASSERTION؛ M1–M7 قدیمی هم در همین محیط دوباره اجرا و 7/7 کشته‌شده شدند (آرتیفکت تاریخی v22.4.1 قبل/بعد پشتیبان‌گیری و بازیابی بایت‌یکسان شد).

## 5. جمع‌بندی

- صحت امنیتی: سه باگ تأییدی رفع و اثبات‌شده (تست + جهش) — «production-ready» صرفاً با همین شواهد ادعا می‌شود.
- کیفیت نرم‌افزار: رگرسیون کامل سبز؛ بسته‌ی قطعی بازتولیدپذیر.
- دقت بنچمارک فریز: **تغییر نگفتنی** (اجرا نشده) — عدد 91.13% تاریخی است.
- کیفیت زبان/تعمیم: ضعف‌های پارچه‌ای اندازه‌گیری و در V23 برنامه‌ریزی شد؛ ادعای «هوش بیشتر» نمی‌کنیم.
