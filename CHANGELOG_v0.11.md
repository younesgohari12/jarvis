# CHANGELOG — JARVIS v0.11.0

## Release 0.11.0 — Bilingual fluency, cognitive routing and stronger research

- مدل Bilingual Fluency روی ۱۰۵ نمونهٔ پروژه‌ساخته در ۱۴ زیرگروه فارسی/انگلیسی آموزش دید. Split موقت ۹۱/۱۴ بود؛ دقت Holdout و پذیرش تولید هر دو `100%` شدند و فقط پس از قبولی، artifact نهایی روی کل داده بازآموزی شد.
- مدل Cognitive Skills روی ۹۶ نمونهٔ دوزبانه و هشت کلاس آموزش دید. Split موقت ۸۰/۱۶ بود؛ دقت Holdout برابر `15/16` یا `93.75%` شد که از کف `87.5%` بالاتر است.
- مدل شناختی فقط نوع مسئله را تشخیص می‌دهد؛ حل عددی و منطقی با الگوریتم‌های قطعی انجام می‌شود تا پاسخ محتمل‌نما جای محاسبه را نگیرد.
- احتمال دوجمله‌ای برای «حداقل»، «حداکثر» و «دقیقاً k» تعمیم یافت. سناریوی ۳۵٪، حداقل دو موفقیت در چهار تلاش اکنون `43.701875%` برمی‌گرداند.
- استدلال روز نسبی از «دیروز» به «پریروز» و صورت انگلیسی تعمیم یافت. منطق قیاسی و مسئلهٔ جعبه‌های با برچسب غلط نیز فارسی/انگلیسی را پوشش می‌دهند.
- خلاصهٔ TCP/UDP هر دو سمت مقایسه را حفظ می‌کند؛ زنجیرهٔ `woman → women → sentence → formal rewrite` و درخواست کوتاه‌سازی متن قبلی اصلاح شد.
- Knowledge آفلاین از ۱۰۱ به ۱۲۵ مدخل دوزبانه رسید و موضوعات آمار، امنیت، پایگاه‌داده، یادگیری ماشین، علوم پایه، اقتصاد و ارزیابی منبع را پوشش می‌دهد.
- بازیابی Knowledge با BM25 مرتب شد؛ Query انگلیسی چندواژه‌ای دیگر به‌خاطر محدودیت ترتیب خام FTS، سند درست را از Candidateها حذف نمی‌کند.
- Search نتیجه‌های Social را مگر با درخواست صریح کاربر حذف می‌کند، صفحه‌های Login/Home و snippetهای کم‌ارزش را کنار می‌گذارد و منابع رسمی/اولیه را بالاتر رتبه می‌دهد.
- Research بازنویسی‌های پزشکی، فنی و آماری تخصصی می‌سازد و شواهد تمام Queryها را به‌جای نگه‌داشتن فقط یک Report، deduplicate و ادغام می‌کند.
- یازده تست بازگشتی v0.11 کیفیت مدل‌ها، مسائل گزارش‌شده، انگلیسی روان، Context، Knowledge BM25، فیلتر Social و ادغام چندمنبعی Research را پوشش می‌دهند.

محدودیت صریح: این آموزش وظیفه‌محور است و JARVIS را هم‌سطح یک LLM بزرگ ابری نمی‌کند. معیارها مربوط به Holdout کوچک و مشخص همین پروژه‌اند، نه ادعای IQ انسانی یا هوش عمومی.

تاریخ Build: 2026-08-31
