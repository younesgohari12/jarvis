# JARVIS v20.1 — اصلاح تطبیق منبع و Slot

مبنای این بسته همان v20 TESTED تحویل‌شده است. فایل‌های v20 و v19 برای بازگشت نگه داشته شده‌اند. این نسخه یک اصلاح متمرکز است؛ ادعای تکمیل ارتقای عمومی زبان، آموزش Transformer یا رسیدن به امتیاز ۸۳–۸۵ ندارد. امتیاز ۸۱٫۶۴ صرفاً ارزیابی گزارش‌شدهٔ شما برای v20 است؛ امتیاز کلی جدید محاسبه نشده است.

## اصلاح‌های اجراشده

- در Work Rate، کارگر و ساعت به‌صورت یک مشاهده به هم متصل می‌شوند. مشاهده‌ای که خروجی معلوم دارد Initial است، حتی اگر در انتهای سؤال بیاید. جای عددها در متن نرمال‌شده در IR ثبت می‌شود.
- Verifier پیش از تأیید جواب، واحدها و رابطهٔ مشاهده‌ها را مستقلاً از متن می‌خواند و با Slotها مقایسه می‌کند. برچسب‌های کش‌شدهٔ Numeric Model برای اثبات درستی خودشان استفاده نمی‌شوند. پوشش نامطمئن یا تعارض، باعث رد تأیید می‌شود.
- پاسخ ردشده دیگر برچسب قدیمی `result_verified` نمی‌گیرد؛ وضعیت شکست تا خروجی حفظ می‌شود.
- Reflection برای ناسازگاری منبع، مرحلهٔ بازسازی Slot را انتخاب می‌کند، حتی اگر مدل Repair مرحلهٔ دیگری پیشنهاد کند. جهان و Graph دوباره ساخته می‌شوند.
- Sequence فهرست اعداد، شمارهٔ جمله و درخواست «عدد بعدی» را جدا ثبت می‌کند. اعداد حاشیه‌ای وارد فهرست نمی‌شوند. برای تشخیص Sequence با اطمینان متوسط، وجود هم‌زمان فهرست صریح و شمارهٔ جمله نقش شاهد تکمیلی دارد. انتخاب خانواده همچنان از مدل آموخته‌شده است؛ این تغییر آموزش مدل جدید نیست.

## شاهد دو باگ

| آزمون | خروجی نسخهٔ اصلاح‌شده |
|---|---:|
| ۳ کارگر، ۴ ساعت، ۸۴ قطعه → ۸ کارگر، ۶ ساعت | 336 |
| دنبالهٔ 24، 12، 6؛ جملهٔ پنجم | 1.5 |
| تزریق workers_initial=8 و workers_target=3 با جواب 47.25 | FAIL در تطبیق منبع |
| اجرای Reflection روی همان IR خراب | 336 و PASS |

جزئیات واقعی Runtime، Slotها، Verdict و مراحل ترمیم در `reports/v20_1/source_verification_evidence.json` است. آزمون محاسبهٔ بهره‌وری به‌تنهایی برای 47.25 پاس می‌شود، اما بررسی معنایی جدید آن را رد می‌کند.

## ارزیابی و حدود اعتبار

| مجموعه | v20 | v20.1 نهایی |
|---|---:|---:|
| همان ۲۷۴ سؤال قبلی، بازآزمون | 197/274 | 215/274 |
| تشخیصی Work Rate | 132/150 | 150/150 |
| تشخیصی Sequence | 72/150 | 150/150 |
| جمع مجموعهٔ تشخیصی | 204/300 | 300/300 |

در هر دو مجموعه، تعداد پاسخ‌های درستِ ازدست‌رفته **صفر** است. آزمون کامل پروژه: **۸۹۹ تست و ۵۸۵ زیرآزمون پاس، صفر شکست**؛ شامل ۲۸ تست جدید برای این اصلاح. این اعداد اثبات بی‌خطا بودن عمومی سیستم نیستند.

مجموعهٔ تشخیصی پیش از اولین اجرا ثابت شد؛ اولین نتیجهٔ اصلاح‌شده 280/300 بود. ۲۰ شکست برای کشف مشکل مسیر قدیمی استفاده شد و نتیجهٔ نهایی بازآزمون 300/300 است. بنابراین آن را Holdout کور و دست‌نخورده نمی‌نامیم. هر ۳۰۰ سؤال یکتا است، اما تنها ۳۰ خانوادهٔ اعلام‌شده و ۲۹ قالب یکتا پس از ماسک عددها دارد. ممیزی ۴۰۷٬۳۹۹ متن آموزشی، هم‌پوشانی دقیق، نرمال‌شده و قالبی را در مقایسه با دادهٔ آموزش صفر گزارش کرد؛ این به‌معنای استقلال مفهومی سؤال‌ها نیست. شرط ۳۰۰ سؤال Fresh عمومی همچنان تکمیل نشده است.

فایل‌های ورودی، همهٔ پاسخ‌ها، شکست‌های اجرای اولیه و مقایسهٔ شناسه‌به‌شناسه در `benchmarks/diagnostic_v20_1_300.jsonl` و `reports/v20_1` موجودند. دادهٔ آزمون به آموزش وارد نشده است. بنچمارک قدیمی ۲۷۴‌سؤالی نیز دیگر Fresh محسوب نمی‌شود.

## عملکرد همین محیط

| معیار | v20 | v20.1 |
|---|---:|---:|
| میانهٔ ساخت Runtime، ms | 123.82 | 117.29 |
| P50 پاسخ، ms | 1.349 | 1.409 |
| P95 پاسخ، ms | 2.570 | 2.633 |
| Peak RSS، MiB | 134.21 | 134.32 |

اندازه‌گیری CPU روی Linux و Python 3.12.13، سه ساخت Runtime و ۱۲۰ درخواست برای پنج سؤال ثابت انجام شد؛ زمان import در Startup این جدول نیست. P50/P95 اندکی بدتر شده‌اند. اختلاف Startup با این نمونهٔ کوچک، دلیل ادعای بهبود پایدار یا رفع افزایش زمان نسبت به v19 نیست. امتیاز Efficiency تازه‌ای تعیین نشده است.

## داده و مدل

تمام 18 فایل checkpoint با پسوند NPZ و تمام 1175 فایل دادهٔ قبلی بایت‌به‌بایت حفظ شدند. در این اصلاح نمونهٔ آموزشی یا checkpoint جدید اضافه نشده است. شمار داده‌های افزودهٔ v20 همان Cognitive=1643، Code=674، Repair=250 و Verification=70 است؛ از Cognitive تعداد 1166 نسبت به مبنای قبلی جدید بود. ادعای ۷۰ هزار نمونهٔ جدید وجود ندارد. فایل‌های گزارش v20 در بسته تاریخی‌اند؛ وضعیت این نسخه در `reports/v20_1/release_gate.json` است.

## اجرا و بازتولید

از پوشهٔ پروژه:

```bash
python -m pip install -r requirements-runtime.txt
python main.py
```

برای آزمون، pytest و pytest-subtests لازم‌اند:

```bash
python -m pip install pytest pytest-subtests
python -m pytest -q
python benchmarks/run_fresh_v20.py . benchmarks/diagnostic_v20_1_300.jsonl reports/v20_1/reproduced_300.json
python benchmarks/run_fresh_v20.py . benchmarks/fresh_v20_clean.jsonl reports/v20_1/reproduced_274.json
```

برای مقایسه با نسخهٔ قبلی، آرگومان اول runner را به مسیر استخراج v20 تغییر دهید و مسیر خروجی جدا بدهید. نام قدیمی runner به‌معنای Fresh بودن دوبارهٔ داده‌ها نیست. ابزارهای خارجی و حافظه در این ارزیابی غیرفعال‌اند. صحت اجرای رابط Windows، GPU و وب زنده در این محیط بررسی نشده است.

Semantic Verification فعلاً برای Work Rate و Sequence افزوده شده و عمومی نیست. Translation/Language، Age، Local Math خارج از این دو مسیر، و General Code Intelligence همچنان نیازمند کار بعدی‌اند. `mission_fully_completed=false` به همین دلیل حفظ شده است.
