# CHANGELOG — JARVIS v0.10.0

## Release 0.10.0 — Trained follow-ups and bounded conversational memory

- یک مدل اختصاصی تشخیص ادامهٔ گفتگو روی ۹۴ نمونهٔ فارسیِ بازبینی‌شده در ۸ کلاس آموزش دید؛ ارزیابی ۸ نمونهٔ کنارگذاشته‌شده را ۸/۸ پاس کرد و سپس artifact نهایی روی کل داده بازآموزی شد.
- حافظه از «کد پروژه» ثابت به Factهای نام‌دار عمومی ارتقا یافت و رمز، کد، شناسه، نام، شماره و آدرس را با مقدار اصلی و Case لاتین ذخیره و بازیابی می‌کند.
- خلاصه‌کردنِ توضیح قبلی مستقیماً از آخرین پاسخ معتبر انجام می‌شود؛ سؤال ناقص دیگر برای پیدا کردن «خلاصه» به وب فرستاده نمی‌شود.
- زنجیره‌های ترجمه مانند `کودک → child → children → جمله → بازنویسی رسمی` به آخرین نوبت معتبر قفل شده‌اند.
- Context ترجمه و موضوع اطلاعاتی دارای عمر یک‌نوبتی است؛ واژه‌ای قدیمی مانند `penis` نمی‌تواند یک درخواست مستقل یا مبهمِ بعدی را آلوده کند.
- سؤال مستقل ترجمه با موضوع صریح همیشه Context قدیمی را جایگزین می‌کند و hard-negativeهای ترجمه وارد دادهٔ آموزش شده‌اند.
- شش تست بازگشتی جدید دقیقاً سناریوی گزارش‌شده، Factهای متفاوت، Context منقضی، جلوگیری از Web Search و Quality Gate مدل را پوشش می‌دهند.
- مجموعهٔ کامل Release شامل ۶۴۵ تست Unit/Integration/Security/Scenario است.

تاریخ Build: 2026-08-28

جزئیات نسخه‌های قبلی در `CHANGELOG_v0.9.md` نگه‌داری شده است.
